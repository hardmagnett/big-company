{
  "files": [],
  "compilerOptions": {
    //    "target": "ESNext",   // чтобы разрешало myStringVariable.replaceAll('px', '') и другие новые возможности.
    "lib": [
      // todo:: удалить эту грязь в комментах.
//      "ES2021.String"
      // Пока-что оставил так. И пусть трахает мозг до полного линтинга.
      // При подсветке в IDE-непонятно. То-ли реально копмилер выдаст проблемы, то-ли просто IDE не подхватывает настройки компилера.
      "ESNext.String" // чтобы разрешало myStringVariable.replaceAll('px', '') и другие новые возможности.
    ]
  },
  "references": [
    {
      "path": "./tsconfig.node.json"
    },
    {
      "path": "./tsconfig.app.json"
    }
  ]
}
