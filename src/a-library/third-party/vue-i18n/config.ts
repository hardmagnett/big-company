import messages from './messages'
export default {
  legacy: false,

  // Сейчас пока-что нужна только русская локаль для перевода сообщений от vuelidate
  locale: 'ru',
  fallbackLocale: 'en',
  messages
}
